const DATABASE_NAME = 'todo_docker_1';//'db_todo';
const USERNAME = 'root';
const PASSWORD = 'root';
const HOST = 'mysqldb';
const DIALECT = 'mysql';
const PORT = '3306';

module.exports = {
  DATABASE_NAME: DATABASE_NAME,
  USERNAME: USERNAME,
  PASSWORD: PASSWORD,
  HOST: HOST,
  DIALECT: DIALECT,
  PORT: PORT,
};